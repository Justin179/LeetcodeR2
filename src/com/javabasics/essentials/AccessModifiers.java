package com.javabasics.essentials;

public class AccessModifiers {
    public static void main(String[] args) {
        // default = package protected = 同目錄下的程式才可以存取(不同package就無法存取)

        // static means this belongs to a class, not an instance
        // used for shared data across all objects





    }
}
